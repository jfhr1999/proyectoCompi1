int funcion(string str){
	int x = 004;
	str = "hola";
	return str;
}

"hola\q" //caracter de escape ilegal, hola ser? un identificador, q ser? un identificador, \ sera un error y cada comilla (") sera otro error

var1-var2 //var1 y var2 seran identificadores y el - un operador

/**
esto es
un comentario
*/

-100 //se separa el operador del numero

hex"5Z" //hex sera palabra reservada y "5Z" sera un literal (string)
hex"10F" //literal (numero hexadecimal)

2.5e-4

'Este es otro String\n'

1.5e //1.5 sera literal (numero) y e sera un identificador

º Á @ \ //caracteres ilegales (error)